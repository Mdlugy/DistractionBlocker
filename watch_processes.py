# run this file to see the names, ids etc. of the active foreground windows. watch the console to see the output. you can then use this information to add to the json files.
from utils.get_process_names import getNames
getNames()
